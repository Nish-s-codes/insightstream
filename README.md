# Insightstream Repository

## Directory Structure
* .gitignore (file)
* app (directory)
    + api (directory)
        - routes.py (file)
    + core (directory)
        - pipeline.py (file)
    + db (directory)
        - vector_store.py (file)
    + services (directory)
        - embed.py (file)
        - ingest.py (file)
        - rag.py (file)
    + main.py (file)
* requirements.txt (file)